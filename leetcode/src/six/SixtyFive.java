package six;

public class SixtyFive {
    /**
     * 1. if only one is breach , then we revise it !
     * 错误
     * 2. this line is generated from modify one node
     * => the point may lift or down !
     *      => for the lift one, once down
     *
     * todo 还是先去刷算法作业
     */
    public boolean checkPossibility(int[] nums) {
        return false;
    }
}
